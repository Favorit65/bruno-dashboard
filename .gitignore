# Секреты — НИКОГДА не коммитить. Токен Bruno живёт только в GitHub Actions Secrets.
*.env
.env
token.txt
*secret*
*token*.txt

# Python
__pycache__/
*.pyc
.venv/
venv/

# ОС/редакторы
.DS_Store
Thumbs.db
.vscode/
.idea/

# Локальные пробные выгрузки (рабочий data.json коммитит робот GitHub Actions).
# Если хочешь коммитить и локальный data.json вручную — убери строку ниже.
/data.local.json

/archive/

# Собранная модель живёт в ветке data-model, а не в main (см. README).
/model.json.gz
/model.json
/model.core.json.gz
/model.cubes.json.gz

# Временные файлы проверок (архивные срезы, пробные модели)
_scratch_*

# Временная папка (device_bash не умеет удалять файлы)
_to_delete/
